---
title: Quebec investors, entrepreneurs and self-employed persons - Canada.ca
url: "https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/quebec-business.html"
category: 移民申请
source: 加拿大政府官网 (IRCC)
date: "2026-07-03"
summary: "The immigration programs for Quebec business class are intended for investors, entrepreneurs, and self employed workers who want to:"
language: en
---

Quebec investors, entrepreneurs and self-employed persons The immigration programs for Quebec business class are intended for investors, entrepreneurs, and self employed workers who want to: settle in Quebec, and support or acquire businesses in Quebec You need to apply to the Quebec government and get a Certificat de selection du Québec before you apply for permanent residence. Fees From [Error loading fee. Check the fee list.] Sections Who can apply Requirements from Quebec and us How to apply Find out how to complete and submit your application After you apply Update your information, submit biometrics and a medical exam Settling in Canada Prepare to arrive or find services if you’re already here Work permit options while seeking permanent residence Find out what type of work permit you can apply for Document navigation Next: Who can apply Print all pages in this section // Function to open all <details> elements function openAllDetails(container) { const detailsElements = container.querySelectorAll("details"); detailsElements.forEach(details => details.setAttribute("open", "")); } // Function to remove the 'mfp-hide' class from all <section> elements function removeMfpHideClass(container) { const sectionElements = container.querySelectorAll("section.mfp-hide"); sectionElements.forEach(section => section.classList.remove("mfp-hide")); } function removeSubwayNav(container) { const subwayElements = container.querySelectorAll("nav.gc-subway"); subwayElements.forEach(subway => subway.remove()); } // Manually resolve data-ajax-* elements since WET won't auto-execute on fetched HTML async function resolveDataAjax(container) { const ajaxTypes = ['replace', 'append', 'prepend', 'after', 'before']; const tasks = []; for (const type of ajaxTypes) { const elements = container.querySelectorAll(`[data-ajax-${type}]`); for (const el of elements) { const fullUrl = el.getAttribute(`data-ajax-${type}`); if (!fullUrl) continue; const [url, hash] = fullUrl.split('#'); if (!url) continue; tasks.push( fetch(url) .then(r => { if (!r.ok) throw new Error(`HTTP ${r.status}`); return r.text(); }) .then(html => { const temp = document.createElement('div'); temp.innerHTML = html; const content = hash ? temp.querySelector(`#${hash}`) : temp; if (!content) return; switch (type) { case 'replace': el.innerHTML = content.innerHTML; break; case 'append': el.appendChild(content.cloneNode(true)); break; case 'prepend': el.insertBefore(content.cloneNode(true), el.firstChild); break; case 'after': el.insertAdjacentHTML('afterend', content.outerHTML); break; case 'before': el.insertAdjacentHTML('beforebegin', content.outerHTML); break; } }) .catch((_e) => {}) ); } } await Promise.all(tasks); } // Resolve data-wb-jsonmanager and data-json-replace elements async function resolveDataJson(container) { const managers = container.querySelectorAll('[data-wb-jsonmanager]'); const jsonMap = {}; await Promise.all(Array.from(managers).map(async el => { try { const config = JSON.parse(el.getAttribute('data-wb-jsonmanager')); const { url, name } = config; if (!url || !name) return; const response = await fetch(url); if (!response.ok) throw new Error(`HTTP ${response.status}`); jsonMap[name] = await response.json(); } catch (_e) {} })); const elements = container.querySelectorAll('[data-json-replace]'); for (const el of elements) { try { const fullRef = el.getAttribute('data-json-replace'); const match = fullRef.match(/^#\[([^\]]+)\]\/(.+)$/); if (!match) continue; const [, name, path] = match; const json = jsonMap[name]; if (!json) continue; const value = path .split('/') .filter(Boolean) .reduce((obj, key) => obj?.[key], json); if (value !== undefined && value !== null) { el.innerHTML = value; } } catch (_e) {} } } // Fetch a page and return its parsed HTML async function fetchPage(url) { const response = await fetch(url); if (!response.ok) throw new Error(`HTTP ${response.status}`); const text = await response.text(); const tempDiv = document.createElement("div"); tempDiv.innerHTML = text; return tempDiv; } // Find the subway index page by crawling up via breadcrumb then URL segments async function findIndexDom() { // Already on index if (document.querySelector("nav.gc-subway dl")) return document; // Try breadcrumb links in reverse (closest parent first) const breadcrumbLinks = Array.from( document.querySelectorAll("ol.breadcrumb li a") ).map(a => a.getAttribute("href")).filter(Boolean).reverse(); for (const url of breadcrumbLinks) { try { const dom = await fetchPage(url); if (dom.querySelector("nav.gc-subway dl")) return dom; } catch (_e) {} } // Fall back to URL segment stripping const parts = window.location.pathname.replace(/\.html$/, '').split('/').filter(Boolean); for (let i = parts.length - 1; i > 0; i--) { const url = '/' + parts.slice(0, i).join('/') + '.html'; try { const dom = await fetchPage(url); if (dom.querySelector("nav.gc-subway dl")) return dom; } catch (_e) {} } return null; } document.getElementById("printAllPages").addEventListener("click", async (e) => { e.preventDefault(); const printButton = document.getElementById("printAllPages"); // Create printContent dynamically if it doesn't exist on the current page let printContent = document.getElementById("printContent"); if (!printContent) { printContent = document.createElement("div"); printContent.id = "printContent"; document.body.appendChild(printContent); } if (!printButton) return; printButton.style.pointerEvents = "none"; printContent.innerHTML = ""; try { const indexDom = await findIndexDom(); if (!indexDom) throw new Error('Could not find subway index page.'); const isOnIndex = indexDom === document; // Strip anchor fragments and deduplicate top level links const topLevelLinks = Array.from( indexDom.querySelectorAll("nav.gc-subway dl dt a") ).map(a => a.getAttribute("href").split('#')[0]) .filter(Boolean) .filter((url, index, self) => self.indexOf(url) === index); if (!topLevelLinks.length) throw new Error('No subway pages found.'); const topLevelResults = await Promise.allSettled( topLevelLinks.map(url => fetchPage(url).then(dom => ({ url, dom }))) ); const expanded = []; const grandchildUrls = new Set(); for (const result of topLevelResults) { if (result.status === 'rejected') continue; const { url, dom } = result.value; expanded.push({ url, dom }); const grandchildren = Array.from( dom.querySelectorAll("nav.gc-subway ul li ul.noline a") ).map(a => a.getAttribute("href").split('#')[0]) .filter(Boolean); for (const gcUrl of grandchildren) { if (!grandchildUrls.has(gcUrl)) { expanded.push({ url: gcUrl, dom: null }); grandchildUrls.add(gcUrl); } } } if (grandchildUrls.size) { const grandchildResults = await Promise.allSettled( Array.from(grandchildUrls).map(url => fetchPage(url).then(dom => ({ url, dom }))) ); const grandchildMap = {}; for (const result of grandchildResults) { if (result.status === 'fulfilled') { grandchildMap[result.value.url] = result.value.dom; } } for (const entry of expanded) { if (entry.dom === null) { entry.dom = grandchildMap[entry.url] || null; } } } // Deduplicate final page list const seen = new Set(); const pages = expanded.filter(entry => { if (!entry.dom || seen.has(entry.url)) return false; seen.add(entry.url); return true; }); // Get index main content — use live DOM if on index, otherwise use fetched DOM const indexMain = isOnIndex ? document.querySelector('main[property="mainContentOfPage"]') : indexDom.querySelector('main[property="mainContentOfPage"]'); if (!indexMain) throw new Error('Main content element not found.'); const [indexPageContent, ...fetchedContents] = await Promise.all([ // Index page Promise.resolve(indexMain.cloneNode(true)) .then(async content => { openAllDetails(content); removeMfpHideClass(content); await resolveDataAjax(content); await resolveDataJson(content); openAllDetails(content); return content; }), // Subpages ...pages.map(({ url, dom }) => Promise.resolve(dom.querySelector('main[property="mainContentOfPage"]')) .then(async fetchedContent => { if (!fetchedContent) return null; removeSubwayNav(fetchedContent); openAllDetails(fetchedContent); removeMfpHideClass(fetchedContent); await resolveDataAjax(fetchedContent); await resolveDataJson(fetchedContent); openAllDetails(fetchedContent); return fetchedContent.cloneNode(true); }) .catch((_e) => null) ) ]); // Append all pages with <hr> separators between them, but not after the last one const allContents = [indexPageContent, ...fetchedContents].filter(Boolean); allContents.forEach((content, i) => { printContent.appendChild(content); if (i < allContents.length - 1) { printContent.appendChild(document.createElement("hr")); } }); const style = document.createElement('style'); style.id = 'temp-print-style'; style.textContent = ` @media print { body > *:not(#printContent) { display: none !important; } #printContent { display: block !important; } body { min-width: 992px; } #printContent main { margin-top: 0 !important; padding-top: 0 !important; } } `; document.head.appendChild(style); document.body.appendChild(printContent); // ensure printContent is a direct child of body at print time window.print(); document.getElementById('temp-print-style').remove(); } catch (_e) { } finally { printButton.style.pointerEvents = ""; printContent.innerHTML = ""; } }); Page details 2026-06-15

---

**原文链接**: [https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/quebec-business.html](https://www.canada.ca/en/immigration-refugees-citizenship/services/immigrate-canada/quebec-business.html)  
**采集时间**: 2026/7/3 19:17:27
